/* INFO  This is my derivation of the Fast Fourier Transform, made as a school project */

fn fft(input: Vec<(f64, f64)>) -> Vec<(f64, f64)> {
    let mut output: Vec<(f64, f64)> = vec![(0.0, 0.0); input.len()];

    /* TODO actual algorithm logic */

    output
}

fn main() {
    let input = vec![(1., 0.), (1., 0.), (1., 0.), (1., 0.)];
}
